# Normalize line endings. Default to LF in the repo; let Git handle checkout.
* text=auto eol=lf

# Shell scripts MUST be LF or they break on Linux/macOS.
*.sh text eol=lf

# Windows-only files stay CRLF.
*.bat text eol=crlf
*.ps1 text eol=crlf
